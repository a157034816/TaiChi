# CentralService base URL (used for Next.js rewrites/proxy)
CENTRAL_SERVICE_BASE_URL=http://localhost:15700

# Cookie name (should match backend config key CentralServiceAuth:CookieName)
AUTH_COOKIE_NAME=CentralServiceAdmin.Auth
