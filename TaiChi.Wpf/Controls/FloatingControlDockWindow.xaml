<Window x:Class="TaiChi.Wpf.Controls.FloatingControlDockWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:TaiChi.Wpf.Controls"
        mc:Ignorable="d"
        Title="FloatingControlDockWindow"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Background="Transparent"
        HorizontalAlignment="Right"
        VerticalAlignment="Bottom"
        Topmost="True">
    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="-1" ResizeBorderThickness="0" CaptionHeight="0" NonClientFrameEdges="None" />
    </WindowChrome.WindowChrome>
    <Window.Template>
        <ControlTemplate TargetType="Window">
            <Border Background="Transparent">
                <!-- <Border> -->
                <!--     <Border.Effect> -->
                <!--         <DropShadowEffect BlurRadius="64" /> -->
                <!--     </Border.Effect> -->
                <!--     <ContentPresenter ClipToBounds="True" /> -->
                <!-- </Border> -->
                <ContentPresenter ClipToBounds="True" />
            </Border>
        </ControlTemplate>
    </Window.Template>
    <Grid x:Name="RootGrid">
        <!-- <Grid.OpacityMask> -->
        <!--     ~1~ <RadialGradientBrush Center="0.5,0.5" RadiusX="0.5" RadiusY="0.5"> @1@ -->
        <!--     ~1~     <GradientStop Color="Black" Offset="0"/> @1@ -->
        <!--     ~1~     <GradientStop Color="Black" Offset="0.9"/> @1@ -->
        <!--     ~1~     <GradientStop Color="Transparent" Offset="1"/> @1@ -->
        <!--     ~1~ </RadialGradientBrush> @1@ -->
        <!--     <LinearGradientBrush StartPoint="0.5,1" EndPoint="0.5,0"> -->
        <!--         <GradientStop Color="Black" Offset="0"/> -->
        <!--         <GradientStop Color="Black" Offset="0.9"/> -->
        <!--         <GradientStop Color="Transparent" Offset="1"/> -->
        <!--     </LinearGradientBrush> -->
        <!-- </Grid.OpacityMask> -->
    </Grid>
</Window>